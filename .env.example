# Database (SQLite) — relative path works on all OS (Windows/Mac/Linux)
DATABASE_URL=file:./db/custom.db

# NextAuth (change the secret in production!)
NEXTAUTH_SECRET=reseller-os-dev-secret-change-in-production
NEXTAUTH_URL=http://localhost:3000
